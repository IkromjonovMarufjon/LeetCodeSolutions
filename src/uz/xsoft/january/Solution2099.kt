package uz.xsoft.january

class Solution2099 {
  /*  fun maxSubsequence(nums: IntArray, k: Int): IntArray {
        var index = 0
        var max = 0
        for (i in 0 until k) {
            max = nums[0]
            for (j in index until nums.size) {
                if (max < nums[j]) {
                    max = nums[j]
                }
            }
        }
    }

    private fun swap(nums: IntArray, index: Int, pos: Int): IntArray {
        val m = nums[index]
        nums[index] = nums[pos]
        nums[pos] = m
        return nums
    }*/
}