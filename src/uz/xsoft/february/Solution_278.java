package uz.xsoft.february;

public class Solution_278 {
    /*public int firstBadVersion(int n) {
        int left = 0, right = n, middle= 0;
        while (left <= right) {
            middle = (left + right) / 2;
            if (isBadVersion(middle)) {
                right = middle -1;
            } else left = middle +1;
        }
        System.out.println(middle);
        if (isBadVersion(middle)) return middle;
        return -1;
    }*/
}
